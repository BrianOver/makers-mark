[gd_scene format=3 uid="uid://cv4atownshell01"]

; V4a additive shell for the town 2.5D migration (plan 2026-07-17-003 §V4a/§V4b).
; Proves the "SubViewport trap" structure: a CanvasModulate inside this SubViewport tints
; ONLY the town world, not the surrounding TabContainer UI. Additive/throwaway like the
; lit_tavern_pilot — NOT wired into town_scene.tscn or main_ui. V4b (4.7.1-gated) migrates
; the live town into a canvas shaped exactly like this.

[node name="TownViewportContainer" type="SubViewportContainer"]
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
stretch = true

[node name="TownViewport" type="SubViewport" parent="."]
handle_input_locally = false
size = Vector2i(1024, 600)
render_target_update_mode = 4

[node name="TownWorld" type="Node2D" parent="TownViewport"]
